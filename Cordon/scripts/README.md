# Scripts

Operational scripts belong here once Cordon’s direct job executor moves to a worker. Repository analysis scripts must call the scanner library and must never import or execute scanned repository code.
