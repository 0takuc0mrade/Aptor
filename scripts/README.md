# Scripts

Operational scripts will live here once contract compilation, local network startup, deployment, and end-to-end tamper tests are implemented.

Scripts must read endpoint configuration from the environment, wait for wallet synchronization, return non-zero exit codes on failure, and never print or persist wallet seeds.
